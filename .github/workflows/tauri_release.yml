name: Tauri Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag (for example `nil-gateway-gui-v0.2.0`)
        required: true
        type: string
  push:
    tags:
      - 'nil-gateway-gui-v*'

permissions:
  contents: write

concurrency:
  group: tauri-release-${{ github.event.inputs.tag || github.ref_name || github.run_id }}
  cancel-in-progress: false

jobs:
  build:
    name: Build Tauri App (${{ matrix.label }})
    runs-on: ${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-15-intel
            label: macos-intel
            bundles: dmg
          - platform: macos-15
            label: macos-apple-silicon
            bundles: dmg
          - platform: ubuntu-22.04
            label: linux-ubuntu-22.04
            bundles: deb,rpm
          - platform: windows-2022
            label: windows-2022
            bundles: nsis,msi

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: nil_gateway_gui/package-lock.json

      - name: Install Dependencies
        working-directory: nil_gateway_gui
        run: npm ci

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Add Windows GNU target
        if: runner.os == 'Windows'
        run: rustup target add x86_64-pc-windows-gnu

      - name: Install Linux system deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Build Tauri
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: nil_gateway_gui
          tagName: ${{ github.event.inputs.tag || github.ref_name }}
          releaseName: NilGateway GUI ${{ github.event.inputs.tag || github.ref_name }}
          releaseBody: "Automated release build."
          releaseDraft: false
          prerelease: false
          args: --bundles ${{ matrix.bundles }}
