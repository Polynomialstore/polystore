pre-commit:
  files: git ls-files polystore-website
  commands:
    lint:
      run: cd polystore-website && npm run lint

pre-push:
  files: git ls-files polystore-website
  commands:
    build:
      run: cd polystore-website && npm run build
