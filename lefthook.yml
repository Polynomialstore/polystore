pre-commit:
  files: git ls-files nil-website
  commands:
    lint:
      run: cd nil-website && npm run lint

pre-push:
  files: git ls-files nil-website
  commands:
    build:
      run: cd nil-website && npm run build
